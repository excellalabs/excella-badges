import * as express from 'express';
declare type WaitingStrategy = (attempt: number) => number;
export declare function createRetriesMiddleware(sessionMiddleware: express.RequestHandler, retries: number, retiesStrategy?: WaitingStrategy): express.RequestHandler;
export {};
